
/* shadow */
inbox-shadow()
  box-shadow inset 0 1px 8px #e8bebe

border-shadow()
  box-shadow inset 0 -1px 0 0 rgba(0,0,0,0.12)

/* border and spliter */
hr(padding = 0, margin = 0)
  width 100%
  display block
  border 1px solid #eee
